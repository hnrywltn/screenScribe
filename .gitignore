# dependencies (unanchored — also matches worker/node_modules)
node_modules

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# local processing scratch (uploaded videos, extracted frames, transcripts)
/storage/

# whisper.cpp model files (hundreds of MB — baked into the worker's
# Docker image at build time in production, not committed here)
worker/models/
